# 采购管理系统字段设计问题分析报告

## 一、问题概述

在检查采购管理系统的数据表设计和前后端代码后，发现存在多个字段设计不合理和前后端不兼容的问题。这些问题会导致数据展示不正确、功能异常等问题。

---

## 二、发现的问题

### 2.1 合同模块 (Contract)

#### 问题 2.1.1：合同类型字段命名不一致

| 位置 | 字段名 | 说明 |
|------|--------|------|
| 后端模型 | `type` (String, 存储如 "NDA保密协议") | 直接存储类型名称 |
| 前端期望 | `contract_type` (Code, 如 "NDA", "Purchase") | 使用代码 |
| 前端表单 | `type_id` (ID) | 使用ID |

**问题描述**：
- 后端 `type` 字段直接存储类型名称（如 "NDA保密协议"），而不是使用类型代码
- 前端在多个地方使用 `contract_type` 字段名，与后端不一致
- 前端在表单中使用 `type_id`，但后端没有对应的关联表

**影响范围**：
- 合同列表展示
- 合同搜索功能
- 合同新增/编辑功能

#### 问题 2.1.2：状态值不一致

| 位置 | 状态值 |
|------|--------|
| 后端模型默认值 | `'active'` |
| 前端显示映射 | `'signed'`, `'draft'`, `'expired'` |
| 前端表单 | `'draft'`, `'signed'`, `'expired'` |

**问题描述**：后端使用 `'active'` 作为默认值，但前端没有处理这个状态值。

---

### 2.2 采购订单模块 (Purchase Order)

#### 问题 2.2.1：状态值不一致

| 位置 | 状态值 |
|------|--------|
| 后端模型默认值 | `'pending'` |
| 前端搜索表单 | `'draft'`, `'sent'`, `'received'`, `'completed'`, `'cancelled'` |
| 前端表单默认值 | `'pending'` |

**问题描述**：
- 后端使用 `'pending'`，但前端搜索表单中没有这个选项
- 前端表单默认值为 `'pending'`，但下拉选项中没有这个选项

#### 问题 2.2.2：字段缺失

| 问题 | 说明 |
|------|------|
| 缺少创建时间 | 后端 `PurchaseOrder` 模型有 `created_at`，但前端表格没有展示 |
| 缺少合同关联名称 | 前端显示供应商名称，但没有显示关联的合同名称 |

---

### 2.3 价格管理模块 (Pricing)

#### 问题 2.3.1：硬编码数据

| 位置 | 问题 |
|------|------|
| 前端搜索表单 (第13-18行) | 硬编码物料选项：CPU、内存、硬盘 |
| 前端表单 (第67-71行) | 硬编码物料选项 |
| 前端表单 (第73-77行) | 硬编码供应商选项：供应商A、供应商B |

**问题描述**：
- 物料和供应商选项都是硬编码的，无法动态获取
- 每次添加新物料或供应商都需要修改前端代码

#### 问题 2.3.2：字段不匹配

| 后端返回 | 前端期望 |
|----------|----------|
| `material_name` | - |
| `supplier_name` | - |
| `effective_date` | 生效日期 |
| `expiry_date` | 过期日期 |

**问题描述**：前端表格直接使用后端返回的字段名，但这些字段名与搜索/表单中的字段名不一致。

---

### 2.4 供应商模块 (Supplier)

#### 问题 2.4.1：状态值未统一

| 位置 | 状态值 |
|------|--------|
| 后端模型默认值 | `'active'` |
| 前端处理 | 未明确处理 |

**问题描述**：前端没有对供应商状态进行明确处理。

---

### 2.5 通用问题

#### 问题 2.5.1：日期格式不统一

| 位置 | 格式 |
|------|------|
| 后端返回 | `'YYYY-MM-DD'` (字符串) |
| 前端表单 | Date 对象 |
| 前端提交 | 需要手动转换 |

**问题描述**：前后端日期格式处理不一致，需要在多处进行格式转换。

#### 问题 2.5.2：ID 类型不统一

| 位置 | 类型 |
|------|------|
| 后端返回 | Integer |
| 前端表单值 | String (el-option 的 value) |
| 前端提交 | 需要转换为 Integer |

**问题描述**：前端使用 `el-select` 时，value 会被转换为字符串，但后端期望 Integer。

---

## 三、解决方案

### 3.1 方案一：统一字段命名（推荐）

#### 3.1.1 合同模块

**修改后端**：
```python
# models/contract.py
# 保持 type 字段，但添加 type_code 用于存储代码
type_code = db.Column(db.String(20))  # 新增：NDA, Purchase 等

# routes/contract.py
# 修改 to_dict 返回
'type': self.type,           # 保持原样：NDA保密协议
'type_code': self.type_code, # 新增：NDA, Purchase
```

**修改前端**：
```javascript
// Contract.vue
// 使用后端返回的 type 字段
contractTypes = [
  { id: 1, code: 'NDA', name: 'NDA保密协议' },
  { id: 2, code: 'Purchase', name: '多物品采购合同' }
]

// 匹配逻辑：根据 name 匹配
const type = contractTypes.find(t => t.name === contract.type)
```

#### 3.1.2 采购订单模块

**统一状态值**：

| 统一后状态值 | 说明 |
|--------------|------|
| `draft` | 草稿 |
| `pending` | 待处理 |
| `sent` | 已发送 |
| `received` | 已接收 |
| `completed` | 已完成 |
| `cancelled` | 已取消 |

**修改后端**：
```python
# models/purchase_order.py
status = db.Column(db.String(20), nullable=False, default='draft')  # 改为 draft
```

### 3.2 方案二：动态获取基础数据

#### 3.2.1 价格管理模块

**添加后端接口**：

```python
# routes/material.py
@bp.route('/', methods=['GET'])
def get_materials():
    materials = Material.query.all()
    return jsonify({
        'data': [m.to_dict() for m in materials],
        'total': len(materials)
    })
```

**修改前端**：

```javascript
// Pricing.vue
const materials = ref([])
const suppliers = ref([])

const getBaseData = async () => {
  const [materialsRes, suppliersRes] = await Promise.all([
    materialService.getMaterials(),
    supplierService.getSuppliers()
  ])
  materials.value = materialsRes.data.data
  suppliers.value = suppliersRes.data.data
}
```

### 3.3 方案三：日期和类型转换中间件

在 API 服务层添加统一的请求/响应处理：

```javascript
// services/api.js
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:5000'
})

// 请求拦截器：转换日期和ID类型
api.interceptors.request.use(config => {
  if (config.data) {
    // 转换日期
    Object.keys(config.data).forEach(key => {
      if (key.includes('date') && config.data[key]) {
        if (config.data[key] instanceof Date) {
          config.data[key] = config.data[key].toISOString().split('T')[0]
        }
      }
      // 转换ID
      if (key.includes('_id') && config.data[key]) {
        config.data[key] = Number(config.data[key])
      }
    })
  }
  return config
})

export default api
```

---

## 四、实施优先级

| 优先级 | 问题 | 预计工作量 |
|--------|------|------------|
| P0 | 价格管理硬编码数据 | 2小时 |
| P0 | 采购订单状态值不一致 | 1小时 |
| P1 | 合同类型字段不匹配 | 2小时 |
| P1 | 日期格式统一处理 | 1小时 |
| P2 | ID类型转换自动化 | 1小时 |

---

## 五、建议

1. **建立数据字典**：统一各模块的字段命名和状态值
2. **添加API文档**：明确每个接口的请求/响应字段
3. **前端抽象**：将通用的数据获取和转换逻辑抽取为可复用函数
4. **后端规范化**：使用枚举或关联表管理状态值，而不是字符串
