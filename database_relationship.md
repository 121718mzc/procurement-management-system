# 采购管理系统数据库逻辑关系图

## 表关系概述

```
+-------------------+       +-------------------+       +-------------------+
| material_categories |<----->|     materials     |<----->|     pricing       |
+-------------------+       +-------------------+       +-------------------+
                                      ^                       ^
                                      |                       |
                                      v                       v
+-------------------+       +-------------------+       +-------------------+
|  contract_types   |<----->|    contracts      |<----->| purchase_orders   |
+-------------------+       +-------------------+       +-------------------+
                                      ^                       ^
                                      |                       |
                                      v                       v
+-------------------+       +-------------------+       +-------------------+
|    suppliers      |<----->|  contract_items   |<----->|purchase_order_items|
+-------------------+       +-------------------+       +-------------------+
                                      ^
                                      |
                                      v
+-------------------+       +-------------------+
|      users        |<----->|    contracts      |
+-------------------+       +-------------------+
                                      ^
                                      |
                                      v
                            +-------------------+
                            | purchase_orders   |
                            +-------------------+
```

## 详细关系说明

### 1. 物料分类与物料
- **关系**：一对多
- **描述**：一个物料分类可以包含多个物料，每个物料属于一个物料分类
- **外键**：`materials.category_id` → `material_categories.id`

### 2. 物料与价格
- **关系**：一对多
- **描述**：一个物料可以有多个价格记录（不同供应商或不同时期），每个价格记录对应一个物料
- **外键**：`pricing.material_id` → `materials.id`

### 3. 供应商与价格
- **关系**：一对多
- **描述**：一个供应商可以提供多个物料的价格，每个价格记录对应一个供应商
- **外键**：`pricing.supplier_id` → `suppliers.id`

### 4. 合同类型与合同
- **关系**：一对多
- **描述**：一个合同类型可以对应多个合同，每个合同属于一个合同类型
- **外键**：`contracts.type_id` → `contract_types.id`

### 5. 供应商与合同
- **关系**：一对多
- **描述**：一个供应商可以签订多个合同，每个合同对应一个供应商
- **外键**：`contracts.supplier_id` → `suppliers.id`

### 6. 用户与合同
- **关系**：一对多
- **描述**：一个用户可以创建多个合同，每个合同由一个用户创建
- **外键**：`contracts.created_by` → `users.id`

### 7. 合同与合同物品
- **关系**：一对多
- **描述**：一个合同可以包含多个物品，每个物品属于一个合同
- **外键**：`contract_items.contract_id` → `contracts.id`

### 8. 物料与合同物品
- **关系**：一对多
- **描述**：一个物料可以出现在多个合同物品中，每个合同物品对应一个物料
- **外键**：`contract_items.material_id` → `materials.id`

### 9. 供应商与采购订单
- **关系**：一对多
- **描述**：一个供应商可以接收多个采购订单，每个采购订单对应一个供应商
- **外键**：`purchase_orders.supplier_id` → `suppliers.id`

### 10. 合同与采购订单
- **关系**：一对多
- **描述**：一个合同可以对应多个采购订单，每个采购订单对应一个合同
- **外键**：`purchase_orders.contract_id` → `contracts.id`

### 11. 用户与采购订单
- **关系**：一对多
- **描述**：一个用户可以创建多个采购订单，每个采购订单由一个用户创建
- **外键**：`purchase_orders.created_by` → `users.id`

### 12. 采购订单与采购订单物品
- **关系**：一对多
- **描述**：一个采购订单可以包含多个物品，每个物品属于一个采购订单
- **外键**：`purchase_order_items.order_id` → `purchase_orders.id`

### 13. 物料与采购订单物品
- **关系**：一对多
- **描述**：一个物料可以出现在多个采购订单物品中，每个采购订单物品对应一个物料
- **外键**：`purchase_order_items.material_id` → `materials.id`

## 数据库设计特点

1. **完整性**：通过外键约束确保数据的完整性和一致性
2. **规范性**：遵循数据库设计的范式，减少数据冗余
3. **可扩展性**：表结构设计考虑了未来的业务扩展需求
4. **性能优化**：为常用查询字段创建了索引，提高查询效率
5. **安全性**：用户密码使用加密存储，保护敏感信息

## 初始数据说明

- **物料分类**：生产原料、电子元件、包装材料、办公耗材
- **合同类型**：NDA保密协议、多物品采购合同
- **用户**：默认采购员账号（用户名：buyer，密码：123456）
- **供应商**：供应商A、供应商B
- **物料**：CPU、内存、硬盘、纸箱、A4纸
- **价格**：为部分物料设置了初始价格
- **合同**：创建了一个示例采购合同
- **采购订单**：创建了一个示例采购订单

## 使用说明

1. **执行SQL脚本**：在MySQL数据库中执行`database.sql`脚本，创建数据库和表结构
2. **验证数据**：执行查询语句验证初始数据是否正确导入
3. **连接应用**：在应用配置中设置数据库连接信息，连接到创建的数据库
4. **测试功能**：使用默认账号登录系统，测试各项功能

此数据库设计满足采购管理系统的核心功能需求，包括供应商管理、物料管理、定价管理、合同管理和采购订单管理。